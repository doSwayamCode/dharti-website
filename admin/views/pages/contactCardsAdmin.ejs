<!-- <h2>Contact Cards</h2>
<a href="/admin/contact-cards/add" class="btn btn-success mb-3"> + Add New Card</a>

<table class="table">
  <thead>
    <tr>
      <th>Order</th>
      <th>Name</th>
      <th>Position</th>
      <th>Email</th>
      <th>Department</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
    <% contactCards.forEach(card => { %>
      <tr>
        <td><%= card.order %></td>
        <td><%= card.name %></td>
        <td><%= card.position %></td>
        <td><%= card.email %></td>
        <td><%= card.department %></td>
        <td>
          <a href="/admin/contact-cards/edit/<%= card._id %>" class="btn btn-warning btn-sm">Edit</a>
          <a href="/admin/contact-cards/delete/<%= card._id %>" class="btn btn-danger btn-sm" onclick="return confirm('Are you sure?')">Delete</a>
        </td>
      </tr>
    <% }) %>
  </tbody>
</table> -->


<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
      <li class="mb-0">Our Team</li>
      <a href="/admin/contact-cards/add" class="btn btn-success">
        <i class="fas fa-plus me-2"></i>Add New Card
      </a>
    </div>
  
    <div class="card shadow-sm">
      <div class="card-body p-0">
        <div class="table-responsive">
          <table class="table table-hover align-middle mb-0">
            <thead class="table">
              <tr>
                <th>Name</th>
                <th>Email</th>
                <th>Department</th>
                <th>Position</th>
                <th width="80">Order</th>
                <th width="100">Actions</th>
              </tr>
            </thead>
            <tbody>
              <% contactCards.forEach(card => { %>
                <tr>
                  <td>
                    <div class="d-flex align-items-center">
                      <img src="<%= card.imageUrl || '/images/default-avatar.jpg' %>" 
                           alt="<%= card.name %>" 
                           class="rounded-circle me-2" 
                           width="40" 
                           height="40">
                      <%= card.name %>
                    </div>
                  </td>
                  <td>
                    <% if (card.email) { %>
                      <a href="mailto:<%= card.email %>" class="text-decoration-none">
                        <%= card.email %>
                      </a>
                      <% } else { %>
                        <span class="text-muted">None</span>
                        <% } %>
                      </td>
                      <td><%= card.department || '-' %></td>
                      <td><%= card.position %></td>
                  <td class="fw-bold"><%= card.order %></td>
                  <td>
                    <div class="d-flex gap-2">
                      <a href="/admin/contact-cards/edit/<%= card._id %>" class="btn btn-sm">
                        <i class="fas fa-edit"></i>
                      </a>
                      <a href="/admin/contact-cards/delete/<%= card._id %>" 
                         class="btn btn-sm" 
                         onclick="return confirm('Are you sure you want to delete this contact card?')">
                        <i class="fas fa-trash"></i>
                      </a>
                    </div>
                  </td>
                  
                </tr>
              <% }) %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
  
  <style>
    .table {
      /* font-size: 0.9rem; */
    }
    .table th {
      /* font-weight: 500; */
      /* text-transform: uppercase; */
      /* font-size: 0.8rem; */
      /* letter-spacing: 0.5px; */
    }
    .table td {
      vertical-align: middle;
    }
    .table img {
      object-fit: cover;
    }
  </style>